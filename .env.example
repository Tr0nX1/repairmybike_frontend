ANDROID_KEYSTORE_BASE64=
ANDROID_KEYSTORE_PASSWORD=
ANDROID_KEY_ALIAS=repairmybike
ANDROID_KEY_PASSWORD=

# Notes:
# - Copy this file to `.env` at the repo root and fill in values.
# - If `.env` is committed and pushed, GitHub Actions will read it.
# - Storing secrets in git is risky; prefer private forks or rotate keys often.